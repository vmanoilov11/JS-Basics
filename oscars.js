function oscars(input){

    let index = 0;
    let name = input[index];
    index++
    let academyPoints = Number(input[index]);
    index++
    let numberOfEvaluators = Number(input[index]);
    index++
    let nameOfEvaluators = input[index]
    index++
    let pointsByEvaluators = Number(input[index])

    for(let i = 0; ;  ){

    }
}